# ServerForge Environment Configuration
# Copy this file to .env and customize for your deployment

# ===========================================
# Server Configuration
# ===========================================
# Port the backend server listens on
PORT=3000

# Node environment (production/development)
NODE_ENV=production

# ===========================================
# Application URLs
# ===========================================
# Public URL where the application is accessible
# For Dokploy: Use your domain (e.g., https://panel1.yourdomain.com)
APP_URL=http://localhost:3000

# Frontend URL (usually same as APP_URL)
FRONTEND_URL=http://localhost:3000

# ===========================================
# Security
# ===========================================
# JWT Secret for authentication tokens
# IMPORTANT: Change this to a random string for production!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-change-this

# ===========================================
# Data Directories (Inside Container)
# ===========================================
# These paths are inside the Docker container
# Use Docker volumes to persist data
DATA_DIR=/app/data
CACHE_DIR=/app/cache
SERVERS_DIR=/app/servers
BACKUPS_DIR=/app/backups

# ===========================================
# Multi-Instance Example
# ===========================================
# Instance 1 (panel1.yourdomain.com):
#   PORT=3000
#   APP_URL=https://panel1.yourdomain.com
#   JWT_SECRET=random-secret-1
#
# Instance 2 (panel2.yourdomain.com):
#   PORT=3000 (same port, different container)
#   APP_URL=https://panel2.yourdomain.com
#   JWT_SECRET=random-secret-2
#
# Each instance will have separate Docker volumes for data isolation
